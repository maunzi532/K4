package effektkarten.sets;

import java.io.*;
import org.junit.*;

public class SpeicherTest
{
	@Test
	public void speichereKartenset() throws IOException
	{
		/*ObjectMapper objectMapper = new ObjectMapper();
		objectMapper.configure(SerializationFeature.WRAP_ROOT_VALUE, true);
		objectMapper.configure(SerializationFeature.FAIL_ON_EMPTY_BEANS, false);
		System.out.println(objectMapper.setVisibility(PropertyAccessor.FIELD, JsonAutoDetect.Visibility.ANY)
				.writerWithDefaultPrettyPrinter().writeValueAsString(new SetV2Aktionen().fertig().alleKarten()));*/
	}
}